// Object.keys
// returns an array of all the keys in an object
// useful when you need to iterate through the keys of an object

let test = { a: 1, b: 2, c: 3 };
let empty = {};
console.log(Object.keys(test));
console.log(Object.keys(empty));
