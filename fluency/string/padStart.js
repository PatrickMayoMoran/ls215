/*
  padStart will add padding to the beginning of string
  default is space, can specify
  if padding length is less than string length, same string is returned
*/

console.log("Meow".padStart(15));
console.log("Meow".padStart(15, '-'));
console.log("Meow".padStart(2, '-'));
