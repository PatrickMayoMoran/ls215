/*
    charCodeAt returns the UTF-16 numerical value of the character at the given index
    If the index is out of bounds, NaN is returned
*/

let tio = 'tio';
console.log(tio.charCodeAt(0));
console.log(tio.charCodeAt(-3));
