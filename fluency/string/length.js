/*
    length is property of string objects that corresponds to the number of
    characters.
*/

let tio = new String("tio");
console.log("Meow".length); // 4
console.log(tio.length); // 3
console.log((55).length); // undefined - number has no length property to read
